{
  "name": "website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:llms": "tsx scripts/generate-llms-txt.ts",
    "assert:release": "tsx scripts/assert-latest-release.ts",
    "predev": "npm run generate:llms && npm run assert:release",
    "prebuild": "npm run generate:llms && npm run assert:release",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "typecheck": "astro check",
    "test": "tsx --test tests/*.test.ts",
    "snapshot:discovery": "tsx ../protocol/scripts/export-discovery-snapshot.ts",
    "snapshot:responses": "tsx ../protocol/scripts/export-opportunity-snapshots.ts"
  },
  "dependencies": {
    "@txnlab/use-wallet": "^5.0.0",
    "@txnlab/use-wallet-defly": "^5.0.0",
    "@txnlab/use-wallet-pera": "^5.0.0",
    "algosdk": "3.6.0",
    "astro": "^5.7.10"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "tsx": "^4.23.13",
    "typescript": "^5.8.3"
  }
}
