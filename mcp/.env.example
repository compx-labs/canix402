# Caddy gateway base URL (never point at raw Fastify upstream)
CANIX402_API_URL=https://canix402-api.compx.io
# Alias used by protocol live/production tests
# X402_PRODUCTION_BASE_URL=https://canix402-api.compx.io

# Must match the gateway / facilitator network identifier
CANIX402_NETWORK=algorand-mainnet

# Opt-in gate for MCP live tests (preflight + metadata; no server-side signing)
# CANIX402_LIVE_TESTS=1
